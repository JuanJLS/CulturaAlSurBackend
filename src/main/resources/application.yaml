spring:
  application:
    name: CulturaAlSur
  datasource:
    url: jdbc:postgresql://localhost:5432/casbackend
    username: admin
    password:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate #update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true